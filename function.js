let arr = ["Bus","Car","nissan",]
function arrSort(arr){
    arr.sort();
    arr.reverse();
    return arr;
}
console.log(arrsort(arr));

//questions
function changeArray(){
    const firstFour = arr.slice(0,4).map(item=>items*8)
    const lastTwo= arr.slice(-2),map(items=>items+5);
    const newArr = firstFour.concat(lastTwo);
    console.log(newArr);
}
